package customer;

import java.time.LocalDate;

public interface ServiceAble {
      int local ();
     Customer[] customer(Customer[] customers);
     Country[] country(Country[] countries);
     Customer[] customer2(Customer[] customers);
}
